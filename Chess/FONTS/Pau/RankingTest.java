package Pau;
/*
import org.junit.Before;
import org.junit.Test;
import org.junit.experimental.theories.suppliers.TestedOn;

import static org.junit.Assert.assertEquals;

public class RankingTest {
    Score s1;
    Ranking r1;

    @Before
    public void setUp(){
        s1 = new Score("Joan",1500);
        r1 = new Ranking(0);
    }

    @Test
    public void constr(){
        assertEquals("Should throw 0",0,r1.getID());
    }

    @Test
    public void addScoreN(){
        assertEquals("Should throw Joan", "Joan", (r1.addScore(s1)).getPlayer());
    }

    @Test
    public void addScoreP(){
        assertEquals("Should throw 1500", 1500, (r1.addScore(s1)).getScore());
    }

}
 */
